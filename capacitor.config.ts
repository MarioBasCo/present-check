import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.stip.fingerprint.app',
  appName: 'fingerprint',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
